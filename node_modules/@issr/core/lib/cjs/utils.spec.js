"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @jest-environment node
 */
var utils_1 = require("./utils");
describe('Utils tests', function () {
    test('isBackend utils test', function () {
        expect((0, utils_1.isBackend)()).toBe(true);
    });
    test('isClient utils test', function () {
        expect((0, utils_1.isClient)()).toBe(false);
    });
    test('clone utils test', function () {
        expect((0, utils_1.clone)({
            app: {
                foo: 'bar',
            },
        })).toStrictEqual({
            app: {
                foo: 'bar',
            },
        });
    });
});
//# sourceMappingURL=utils.spec.js.map